# Discord Bot Token (required)
TOKEN=your_discord_bot_token_here

# SSH access (optional)
SSH_ENABLED=false

# ngrok tunnel for SSH (optional)
NGROK_ENABLED=false
NGROK_AUTHTOKEN=your_ngrok_auth_token_here

# Build-time args (used in build_image.sh)
INSTALL_NGROK=false
